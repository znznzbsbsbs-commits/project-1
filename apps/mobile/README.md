# Liquid Messenger Mobile

React Native client with real API login, chat loading, WebSocket realtime updates and message sending against the same hosted backend.

## Run

```bash
cd apps/mobile
npm install
LIQUID_MESSENGER_URL=http://localhost:8080 npm start
```

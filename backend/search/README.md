# search service

This folder contains the service contract in `service.json`. The runtime implementation is mounted in the production API gateway so the deployed platform has a single hardened HTTP/WebSocket entry point while preserving service boundaries, route ownership, tables and security requirements.

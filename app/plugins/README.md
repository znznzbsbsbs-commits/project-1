# Plugin source workspace

Use this folder for local extension source code before packaging it as `.myext` or publishing it to the marketplace. A runtime extension must contain `manifest.json`, an `entry` module that exports `activate(api)`, optional assets and only use the official SDK permissions.
